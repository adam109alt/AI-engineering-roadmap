# Pre-trained Models

It's an models that have been trained on a huge amount of parameters/data, And some exmaples of these models:

**Claude, Gemini, GPT, Mistral, DeepSeek, etc** 

And these models are trained on *unsupervised data* to make the model discover hidden structures, shapes, So the AI try to discover the next word and these things to make him like *Human-possiable*

And we AI enigneers can get to these models by API's

And we can also **fine-tune** the model by traning the model on a new data to make it very **good** in specific task
